import { Icon } from './icons.jsx'
import { business } from '../data/business.js'
import { featuredReview, testimonials, summaryQuotes } from '../data/testimonials.js'

export default function Reviews() {
  return (
    <section id="reviews" className="bg-cream-100 py-20">
      <div className="container-page">
        <div className="flex flex-wrap items-end justify-between gap-6">
          <div className="max-w-xl">
            <p className="eyebrow">Reviews</p>
            <h2 className="section-title mt-3">What Belfair says</h2>
          </div>
          <div className="flex items-center gap-3">
            <div className="flex" aria-hidden="true">
              {Array.from({ length: 5 }).map((_, i) => (
                <Icon key={i} name="star" className="h-5 w-5 fill-rust-500 text-rust-500" />
              ))}
            </div>
            <span className="font-mono text-sm text-graphite/60">
              {business.rating} &middot; {business.reviewCount} Google reviews
            </span>
          </div>
        </div>

        <div className="mt-12 grid gap-8 lg:grid-cols-5">
          <div className="rounded-md bg-racing-700 p-8 text-cream lg:col-span-3 lg:p-10">
            <Icon name="quote" className="h-8 w-8 text-rust-400" />
            <p className="mt-4 text-xl leading-relaxed sm:text-2xl">
              &ldquo;{featuredReview.quote}&rdquo;
            </p>
            <div className="mt-6 flex items-center justify-between">
              <div>
                <p className="font-display font-bold uppercase tracking-wide text-cream">
                  {featuredReview.author}
                </p>
                <p className="font-mono text-xs text-cream/50">{featuredReview.meta}</p>
              </div>
            </div>
            {featuredReview.ownerReply && (
              <div className="mt-6 rounded-sm border-l-4 border-rust-400 bg-racing-800/60 p-4">
                <p className="font-mono text-xs uppercase tracking-wide text-cream/50">
                  Shop's reply
                </p>
                <p className="mt-1 text-sm text-cream/80">&ldquo;{featuredReview.ownerReply}&rdquo;</p>
              </div>
            )}
          </div>

          <div className="space-y-6 lg:col-span-2">
            {testimonials.map((t) => (
              <div key={t.author} className="rounded-md border border-graphite/10 bg-white p-6">
                <p className="leading-relaxed text-graphite/80">&ldquo;{t.quote}&rdquo;</p>
                <p className="mt-4 font-display text-sm font-bold uppercase tracking-wide text-graphite">
                  {t.author}
                </p>
                <p className="font-mono text-xs text-graphite/45">{t.meta}</p>
                {t.ownerReply && (
                  <p className="mt-3 border-t border-graphite/10 pt-3 text-xs italic text-graphite/50">
                    Shop's reply: &ldquo;{t.ownerReply}&rdquo;
                  </p>
                )}
              </div>
            ))}
          </div>
        </div>

        <div className="mt-8 flex flex-wrap gap-3">
          {summaryQuotes.map((q) => (
            <span key={q} className="tag-chip normal-case">
              <Icon name="quote" className="h-3.5 w-3.5" />
              {q}
            </span>
          ))}
        </div>
        <p className="mt-3 font-mono text-[11px] uppercase tracking-wide text-graphite/40">
          Highlight quotes generated by Google from this business's real
          review history
        </p>
      </div>
    </section>
  )
}
